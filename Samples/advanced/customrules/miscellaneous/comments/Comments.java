/**
 *
 * FIXME: This class is incomplete.
 *
 */
public class Comments {
  public static void main (String [] args) {
    try {
      if (0 == args.length) {
        return;
      }
      if (1 == args.length) {
        /* FIXME: This is completely broken.
           Need to parse the command line better */
      }
    }
    catch (Exception e) {
      // FIXME: Need to enumerate all exceptions and handle them
    }
  }
}
